<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de mascotas</title>
    
   
</head>
<body>
    <div class="container">
        {% for datoMascota in datoMascota %} {#Se hace un bucle for para recorrer el array de mascotas#}
            <h3>Name: {{datoMascota.name}}</h3>
            <h3>Species: {{datoMascota.species}}</h3>
            {%if {{datoMascota.favFoods}} is defined %}
                <h3>Favourite food:</h3>
                    <li>
                        {%for comidaFav in {{datoMascota.favFoods}} %}
                            <ul>comidaFav</ul>
                        {%endfor%}
                    </li>
            {%endif%}
            <h3>Birth year: {{datoMascota.birthYear}};
            <img src="asset{{datoMascota.photo}}" alt="Pets Image">
        {% endfor %} {#Se cierra el bucle for#}
    </div>
</body>
</html>
